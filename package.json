{
  "name": "test1",
  "version": "1.0.11",
  "main": "main.js",
  "license": "MIT",
  "scripts": {
    "start": "electron .",
    "build": "electron-builder build --mac --win --publish never",
    "deploy": "electron-builder build --mac --win --publish always"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/CallBackWJ/electron-test.git"
  },
  "dependencies": {
    "electron-updater": "^4.2.2"
  },
  "devDependencies": {
    "electron": "^13.6.6",
    "electron-builder": "^22.3.2"
  }
}
